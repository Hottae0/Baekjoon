# .github/workflows/main.yml
name: Auto Update Readme
on:
  schedule:
    - cron: "0 15 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: PS Stat Update
        # 여기가 변경되었습니다!
        uses: juhyung-p/baekjoon-stat-action@v1.0.2
        with:
          # 본인의 백준 아이디를 입력하세요
          user_name: hottae00311
          
          # 템플릿과 결과 파일 모두 README.md로 지정
          template_path: "README.md"
          readme_path: "README.md"
          
          # 커밋 메시지
          commit_message: "🏆 chore: Update solved problem list"
